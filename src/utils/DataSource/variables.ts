import { Sport, Player, Team, Match } from 'config/types';

export const sports: {[sportName: string]: Sport} = {}
export const players: Player[] = []
export const teams: {[teamName: string]: Team} = {}
export const matches: Match[] = []