{
  "name": "mvp_calc",
  "version": "1.0.0",
  "description": "MVP calculator",
  "main": "src/index.ts",
  "author": "Ahmed elSayed",
  "license": "ISC",
  "scripts": {
    "start": "export NODE_PATH=./src && npm run build && node build/index.js",
    "dev": "export NODE_PATH=./src && nodemon",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.0.6",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.3"
  }
}
